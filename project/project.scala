object project extends ProjectSettings {
  def scalaVersion = "2.10.5"
  def version = "1.2.0"
  def name = "i18n"
  def description = "Rapture I18N is a simple library for working with internationalized strings"
  
  def dependencies = Seq(
  )
  
  def thirdPartyDependencies = Nil

  def imports = Seq(
  )
}
